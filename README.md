# Simulator

Firstly, use Visdom

```
python -m visdom.server
```

For training

```
python main.py
```

For testing

```
python main.py --test
```

For tracing loss curves, just enter `http://localhost:8097` into browser

